<!DOCTYPE html>
<html>
<head>
  <title>WhatsApp Bot Admin</title>
  <style>
    body { font-family: sans-serif; padding: 20px; background: #f0f0f0; }
    table { width: 100%; border-collapse: collapse; background: white; }
    th, td { padding: 10px; border: 1px solid #ccc; }
    th { background: #333; color: white; }
  </style>
</head>
<body>
  <h2>📊 WhatsApp Bot Usage Dashboard</h2>
  <table>
    <tr>
      <th>User ID</th>
      <th>Queries Used</th>
      <th>Reset Time</th>
      <th>Paid User</th>
    </tr>
    <% users.forEach(u => { %>
      <tr>
        <td><%= u.id %></td>
        <td><%= u.count %></td>
        <td><%= new Date(u.resetAt).toLocaleString() %></td>
        <td><%= u.paid ? "✅" : "❌" %></td>
      </tr>
    <% }) %>
  </table>
</body>
</html>
